同频信号测相位差的思路如下：
要得到相位差，那就要知道两个上升沿之间的差值,这是相位之间的差值，但是仅知道相位差，不知道周期也是得不到相位的角度。
因此需要测量信号的频率，只需要测量其中一个就可以。
即，在A的上升（下降）沿开始计数，在B的上升（下降）沿停记录计数的值T1，这个计数值就是相位差。继续计数，在下一个A的上升沿（下降）沿停止计数，记录计数值T2
则相位差角度 = T1/T2 *360°
由于不能调用COS函数，因此列出了361个角度值，由于1度之间的角度精确到第四位小数，因此将这个值扩大10000倍保存用于余弦的计算